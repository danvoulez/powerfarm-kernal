FROM python:3.12-slim
WORKDIR /app
COPY pyproject.toml ./
COPY kernel kernel
COPY agent agent
RUN pip install --no-cache-dir .
# ADK wiring lands in Phase 5; the agent container only ever proposes Commands.
CMD ["python", "-c", "import time; time.sleep(10**9)"]
